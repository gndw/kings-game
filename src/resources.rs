//! ECS resources — the map border, the building roster, the calendar, the
//! date that walks it, the chronicle log, and the active input layer.

pub mod border;
pub mod buildings;
pub mod calendar;
pub mod chronicle;
pub mod date;
pub mod input_layer;
